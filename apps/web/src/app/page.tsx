'use client';

import Layout from './components/Layout';

const Page = (): JSX.Element => (
  <Layout>
    <button onClick={() => null}>Logout</button>
  </Layout>
);

export default Page;
