{
  "name": "@repo/utils",
  "version": "0.0.1",
  "private": true,
  "main": "./src",
  "types": "./src",
  "scripts": {
    "build": "rm ./src/*.js && tsup src --format cjs --outDir src"
  },
  "devDependencies": {
    "tsup": "^8.0.2",
    "typescript": "^5.3.3",
    "@repo/eslint-config": "*",
    "@repo/typescript-config": "*",
    "@types/node": "^20.11.24",
    "@types/react": "^18.2.61",
    "@types/react-dom": "^18.2.19"
  }
}